apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: web-content
    files:
      - index.html=./index.html
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
